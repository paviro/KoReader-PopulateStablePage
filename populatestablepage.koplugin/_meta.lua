local _ = require("gettext")

return {
    name = "populatestablepage",
    fullname = _("Populate missing stable page metadata"),
    description = _("Populate missing stable page metadata so KoShelf and similar tools can map reading statistics."),
}

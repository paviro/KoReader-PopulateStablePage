local _ = require("gettext")

return {
    name = "populatestablepage",
    fullname = _("Populate stable page metadata"),
    description = _("Populate stable page metadata so KoShelf and similar tools can map reading statistics."),
}
